
; global prelude macros and fns

(fun mahfun (x y)
    (* x y 10))

(sys.test 'fun
    (= 120 (mahfun 3 4)))

(sys.test 'isa?
    (= true (isa? mahfun 'fn)))

(sys.test 'pair?
    (= true (pair? '(1 2))))

(sys.test 'let
    ((fn ()
        (def foo
            (let loop (n 0)
                (if (= n 10)
                    n
                    (loop (+ n 1)))))
        (= foo 10))))

;(sys.test 'cond false)

