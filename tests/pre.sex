
; global prelude macros and fns

(fun mahfun (x y)
    (* x y 10))

(test 'fun
    (= 120 (mahfun 3 4)))

(test 'isa?
    (= true (isa? mahfun 'fn)))

(test 'pair?
    (= true (pair? '(1 2))))

(test 'let
    ((fn ()
        (def foo
            (let loop (n 0)
                (if (= n 10)
                    n
                    (loop (+ n 1)))))
        (= foo 10))))

;(test 'cond false)

