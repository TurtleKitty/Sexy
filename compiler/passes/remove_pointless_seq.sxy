
(use gen-pass "generic.sxy")

(lib () pass)

; (seq expr) -> expr

(def pass
    (gen-pass (:
        'seq (proc (form)
                (if (= form.size 2)
                    (pass form.1)
                    %(seq @(pass form.tail)))))))

