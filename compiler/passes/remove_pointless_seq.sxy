
(lib () pass)

; (seq expr) -> expr

(proc pass (form)
    (if (pair? form)
        (if (and (= form.head 'seq) (= form.size 2))
            (pass form.1)
            (pair (pass form.head) (pass form.tail)))
        form))

